# Reference Uploads

Raw material pasted/uploaded over the course of the project — not build output, just source references. Kept as-is rather than sorted further since most are one-off reference screenshots.

Worth flagging specifically:
- **`sorbetes_full_flow (3).html`** (3MB) — despite the generic name, this is actually a complete prototype of the **"For Walk-Ins" page** (`<title>Sorbetes — For Walk-Ins</title>`), the same page `FRONTEND_UPDATE.md` §5 specs as a NEW page to build (`WalkInsInfo.jsx`). Worth checking against that spec before starting that page from scratch.
- **`order form.docx`** — source order form.
- **Fit/collar reference photos** (`standard fit standard collar.png`, `oversize fit proclub collar.png`, etc.) — visual references for the collar/fit options in `orderConfig.js`.
- **`pasted-*.png`** (~70 files) — chat-pasted screenshots/references, chronological by filename timestamp, uncategorized.
- **`josh-model.png`** — identical to `01-Website-Prototype/uploads/Josh Model.png`.
