# Cleanup changelog

Original zip: 227 files, 114MB → Cleaned: 201 files, 99MB.
Your original upload is untouched — this is a new, separate structure built from it.

## Folder structure
- `01-Website-Prototype/` — the full site (was `Sorbetes Apparel Website/`): Home, Portfolio, Mockup, Quick Quote, Get Quote, GWQ*, auth, dashboard, `HANDOFF.md`, `SYSTEM-FLOW-SPEC.md`
- `02-ScrollCine-Demo-Build/` — packaged Dashboard + Order Flow demo with bundled React (was `Sorbetes_ScrollCine_v3_Site/`)
- `03-Order-Flow-Export-Formats/` — the 5 export formats of the root-level Order Flow prototype, with a README explaining which to use when
- `04-Frontend-Update-React-Code/` — the actual production JSX/CSS to paste into `sorbetes-frontend` (was `frontend-update/` + `FRONTEND_UPDATE.md`)
- `05-Reference-Uploads/` — raw pasted screenshots, `order form.docx`, fit/collar reference photos, etc. (was the loose top-level `uploads/`)

## What changed inside 01-Website-Prototype
Several pages existed twice — a plain-named file and a `-{hash}` twin — from two export sessions merged into one zip. In every case I checked, the hash-suffixed copy was the newer one (real nav links instead of placeholder `#`, the corrected CTA routing per `HANDOFF.md`'s updated rule, extra features). **Kept the newer content, saved under the plain filename** (since that's what every page's internal links point to), and dropped the older twin:

| Kept (newer content) | Older duplicate dropped |
|---|---|
| `Home.dc.html` | `Home.dc-d6906cd3.html` |
| `Mockup.dc.html` | `Mockup.dc-2e774129.html` |
| `Portfolio.dc.html` | `Portfolio.dc-2f1acd1b.html` |
| `Quick Quote.dc.html` | `Quick Quote.dc-d8e38984.html` |
| `Get Quote.html` | `Get Quote-b942a37f.html` (added back-link, "Continue order →", and quote-summary localStorage save) |
| `HANDOFF.md` | `HANDOFF-05de5edc.md` |
| `support.js` | `support-78f6b3cb.js` (this is the design tool's runtime engine, not your code) |
| `image-slot.js` | `image-slot-4cffaf8e.js` (byte-identical, no content decision needed) |

Also byte-identical duplicate assets, kept one copy: `Josh Model.png`, `Logo too.jpg`, `Adobe Express - file (5).png`, `sorbetes-quote-builder V3.html`.

Two `.thumbnail` files (WebP cache thumbnails generated by the design tool) were dropped — not real content.

**Moved to `01-Website-Prototype/_experimental-and-archive/`** rather than deleted:
- `Get Quote v2.html` — `SYSTEM-FLOW-SPEC.md` §5 item 9 documents this as "a parallel experimental artifact (immersive scroll-scrub redesign, awaiting model video), not wired into any flow." Kept for reference, out of the main folder since it's not part of the live site.
- `screenshots/` — `HANDOFF.md` itself calls these "debug captures, safe to ignore/delete." Archived rather than deleted just in case.

## What changed inside 02-ScrollCine-Demo-Build
Same plain-vs-hash duplication pattern, but here **every pair was byte-identical** (this folder was re-saved as a whole unit, not edited) — no content decisions needed, just kept the plain-named copy of each: `Dashboard.dc.html`, `Sorbetes Order Flow.dc.html`, `index.html`, `support.js`, `image-slot.js`, both `vendor/react*.js` files, and 6 image assets in `uploads/`.

## 03-Order-Flow-Export-Formats
These 5 files aren't duplicates of each other — they're different **export formats** of the same prototype (raw editable source vs. dependency-tagged standalone vs. fully self-contained single-file bundles). Nothing removed here, just moved into one folder together with the sibling files they need (`Dashboard.dc.html`, `support.js`, `image-slot.js`, `fabric-colors.js`, and only the image/video assets they actually reference — not the full loose `uploads/` folder). See that folder's `README.md` for which format to use when. The two root-level debug screenshots (`action-bar-check.png`, `collar-zoom.png`) moved here too, into `_screenshots/`.

## Worth flagging (not a duplicate, but easy to miss)
`05-Reference-Uploads/sorbetes_full_flow (3).html` — despite the generic name, this is a complete prototype of the **"For Walk-Ins" page** (`<title>Sorbetes — For Walk-Ins</title>`) — the same page `FRONTEND_UPDATE.md` §5 specs as a page still to be built (`WalkInsInfo.jsx`). Worth checking against that spec before building that page from scratch.

`_docxml.txt` (now in `05-Reference-Uploads/`) is a raw Word-XML dump of the same fields already in `order form.docx` — redundant, safe to ignore.

## Not touched
I didn't fix any of the actual site bugs `SYSTEM-FLOW-SPEC.md` already documents (broken `Fill Form Directly.dc.html` link, dead nav links, no auth gate, etc.) — those are product decisions, not duplicate-file cleanup, and the doc already has them logged with a recommended fix in §6.

## Update: v8 Order Flow + consolidated build spec added
A second, separate upload later provided `Sorbetes Order Flow (v8 - CURRENT).html` — a newer, smaller rebuild of the ordering flow (auth switched to Google OAuth, payment split changed to 60%/40%, matching `Sorbetes_Flowchart_v8.pdf` in `05-Reference-Uploads`). It's now added to `03-Order-Flow-Export-Formats/` as the clearly-marked current reference. The two older giant bundled files that used to sit there (`Sorbetes Order Flow (Standalone).html`, `Sorbetes Order Flow (offline).html`, pre-v8, both show the old 50/50 split) moved to `03-Order-Flow-Export-Formats/_superseded-pre-v8/` — kept for history, not for building from.

Also added at the root: **`FRONTEND-BUILD-SPEC.md`** — a consolidated, reconciled build spec written after comparing every source doc in this project (the v8 flowchart, `FRONTEND_UPDATE.md`, `orderConfig.js`, `SYSTEM-FLOW-SPEC.md`). It resolves the 50/50-vs-60/40 conflict, locks the auth model (Google OAuth only, confirmed — no email/password fallback), and scopes the build to frontend-only (no backend exists yet, so integration points should be mocked). This is the single doc to hand to whoever builds the real React app — it supersedes reading the individual source docs separately, since a couple of them now conflict with each other and this doc states which one wins.
